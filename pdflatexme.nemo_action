[Nemo Action]
Name=XXXtex Me
Comment=Run Pdflatex, bibtex, pdflatex on a file

Exec=gnome-terminal --working-directory=%P -x sh -c "pdflatex %f"

Icon-Name=bug-buddy

Selection=Any

Extensions=tex;

Quote=double
